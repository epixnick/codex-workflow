{
  "name": "codex-workflow-bootstrap",
  "private": true,
  "version": "0.0.0",
  "description": "Minimal package scaffold so default workflow verification commands can run.",
  "packageManager": "pnpm@10.30.1",
  "scripts": {
    "build": "node -e \"console.log('build ok')\"",
    "typecheck": "node -e \"console.log('typecheck ok')\"",
    "test": "node -e \"console.log('test ok')\""
  }
}
